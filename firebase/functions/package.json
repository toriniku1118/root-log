{
  "name": "rootlog-functions",
  "private": true,
  "version": "0.1.0",
  "main": "lib/index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "rm -rf .test-build && tsc -p tsconfig.test.json && node --test \".test-build/**/*.test.js\"",
    "test:integration": "rm -rf .integration-build && tsc -p tsconfig.integration.json && node --test --test-concurrency=1 \".integration-build/**/*.test.js\""
  },
  "dependencies": {
    "exif-reader": "^2.0.0",
    "firebase-admin": "^14.5.0",
    "firebase-functions": "^7.4.0",
    "sharp": "^0.35.4"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "type": "module"
}
